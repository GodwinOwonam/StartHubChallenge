/**
 * Task five
 */

while(true){
    var visitorResponse = prompt("Enter a number greater than 500 or an empty line to cancel this prompt:");

    if(visitorResponse > 500 || visitorResponse ==="") break;
}